export const devEndpoint = `http://localhost:3000/api/graphql`
export const prodEndpoint = `production graphql endpoing here`
export const unAuthRedirect = {
  redirect: {
    destination: "/login",
    permanent: false,
  },
}
