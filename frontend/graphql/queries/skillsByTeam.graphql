query skillsByTeam($teamId: ID!) {
  skills(
    where: { team: { id: { equals: $teamId } } }
    orderBy: { created: asc }
  ) {
    id
    name
    description
    levels(orderBy: { level: desc }) {
      id
      name
      description
      level
    }
  }
}
